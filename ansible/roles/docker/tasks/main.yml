---
- name: Include role variables
  ansible.builtin.include_tasks: "include_vars.yml"
  tags:
    - always
  loop:
    - "{{ ansible_os_family }}.yml" # e.g. Debian for ubuntu and debian
  loop_control:
    loop_var: vars_file

- name: Install docker
  ansible.builtin.include_tasks:
    file: install-docker/main.yml
    apply:
      tags:
        - docker
  tags:
    - docker

- name: Install watchtower
  ansible.builtin.include_tasks:
    file: watchtower/main.yml
    apply:
      tags:
        - watchtower
  when: boolean_docker_watchtower_enabled
  tags:
    - watchtower
