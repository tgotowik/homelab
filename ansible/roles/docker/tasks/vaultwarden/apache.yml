---
- name: Copy apache2 conf
  ansible.builtin.template:
    src: etc/apache2/sites-available/bw.tgotowik.de.conf.j2
    dest: /etc/apache2/sites-available/bw.tgotowik.de.conf
    owner: root
    group: root
    mode: "0640"

- name: Enable site bw.tgotowik.de
  ansible.builtin.file:
    src: /etc/apache2/sites-available/bw.tgotowik.de.conf
    dest: /etc/apache2/sites-enabled/bw.tgotowik.de.conf
    state: link
  notify: restart apache2